1. On private channel click:
    1.1 Load first 50 messages.
    1.2 Show messages.
    1.3 Show input field.
        1.3.1  Send message.
2. Show guilds:
    2.1 Show channels.
    2.2 Same messaging logic as for private channels.

Optimization todos:
1. Image Caching Issue:
    1.1 On every UI update all images are loaded from disk and creates Image objects.
2. Avoid Duplicating Strings:
    2.1 On every update Rust String is converted to Slint SharedString. 
        Since SharedString is reference-counted, this creates unnecessary allocations.
3. Reduce Update Frequency:
    3.1 The update channel sends (), which updates the entire UI.
        Instead only send new data.
4. Cache messages.
5. State Duplication.
6. Clean up in cached avatar images. 